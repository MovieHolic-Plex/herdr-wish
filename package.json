{
  "name": "herdr-wish",
  "version": "0.2.1",
  "private": true,
  "description": "Herdr plugin: force omo /wish on a new worktree, then commit and PR. omo-10 opens 10 worktrees.",
  "license": "MIT",
  "scripts": {
    "test": "node --test wish.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
